spring:
  application:
    name: config-client-test
  profiles:
    active: dev,prod
  cloud:
    config:
      env: default # optional
      label: master # optional
      uri: http://localhost:8888 # optional