server:
  port: 8081

spring:
  application:
    name: demo-poc
  profiles:
    active: dev
  cloud:
    config:
      #env: prod
      label: tag-demo
      uri: http://localhost:8888

# afin de permettre le refresh pas l'actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"